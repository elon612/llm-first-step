# Offline lessons (tokenize + next-token demo) use the Python standard library only.
# Install these extras if you want a real tokenizer and a live API call.

tiktoken>=0.8.0
httpx>=0.27.0
python-dotenv>=1.0.0
